# PID 调参脚本使用说明

## 通用前置条件

运行前先进入脚本目录并加载 ROS 环境：

```bash
cd /home/raybot/raybot_pid_ws
source /opt/ros/humble/setup.bash
source /home/raybot/raybot_core_ws/install/setup.bash
```

需要确认：

- `/cli_controller/execute_command` 可用，供单关节运动测试使用。
- `/r_arm_controller/controller_state` 和 `/l_arm_controller/controller_state` 正常发布。
- 自动调 `Kp` 时，`ethercat` 命令可读写驱动参数。
- 完整 YAML 任务调参时，`/action_recorder/edit` 和 `/action_recorder/command` 可用。

关节名和 EtherCAT alias 自动映射：

- 左臂：`left_arm_1_joint` 到 `left_arm_7_joint`，alias 为 `1102..1108`。
- 右臂：`right_arm_1_joint` 到 `right_arm_7_joint`，alias 为 `1109..1115`。

---

## 1. `right_1_arm.py`

用途：单个关节运动测试，或基于 3 个明确目标角度调单个关节 `Kp`。

不适合：完整 motion program YAML 任务级评价。完整 YAML 请用 `task_motion_program_kp_tune.py`。

### 单次运动测试

```bash
python3 right_1_arm.py 0.5
```

指定其他关节：

```bash
python3 right_1_arm.py --joint-name left_arm_3_joint 0.3
```

常用参数：

- `--joint-name`：目标关节，默认 `right_arm_1_joint`。
- `--vel`：速度缩放，默认 `0.3`。
- `--acc`：加速度缩放，默认 `0.3`。
- `--no-plot`：只输出指标，不保存图。
- `--plot-output`：指定响应图路径。

输出：

- 终端输出 `rmse`、`phase_lag`、`amplitude_ratio`、`overshoot`、`search_cost`。
- 默认保存单次响应图，例如 `right_arm_1_joint_response_0_5.png`。

### 单关节自动调 Kp

交互输入 3 个目标角度：

```bash
python3 right_1_arm.py --joint-name right_arm_2_joint --auto-tune-kp
```

非交互指定 3 个目标角度：

```bash
python3 right_1_arm.py --joint-name right_arm_2_joint --auto-tune-kp \
  --auto-tune-actions=-0.2,0.0,0.2
```

常用搜索参数：

- `--kp-iterations 9`：快速验证，最低 9。
- `--kp-iterations 12`：默认推荐预算。
- `--kp-min / --kp-max`：手动限定搜索区间。
- `--kp-range`：围绕当前 `Kp` 给定绝对搜索半径。
- `--kp-hard-max`：限制最高可试 `Kp`。

输出：

- `kp_trials_raw.csv`：每个 `Kp x action x repeat` 的原始 trial。
- `kp_trials_summary.csv`：每个候选 `Kp` 的聚合结果。
- `*_preview.png`：原始 `Kp` 的动作预览图。
- `*_comparison_*.png`：原始 `Kp` 与最佳 `Kp` 对比图。

推荐用法：

```bash
python3 right_1_arm.py --joint-name right_arm_2_joint --auto-tune-kp \
  --auto-tune-actions=-0.2,0.0,0.2 \
  --kp-iterations 9
```

---

## 2. `auto_tune_arm_joints.py`

用途：批量顺序调用 `right_1_arm.py`，对多个关节做“单关节、明确动作”的 `Kp` 调参。

不适合：完整 motion program YAML 任务级评价。完整 YAML 请用 `task_motion_program_kp_tune.py`。

### 按配置文件批量调参

动作配置示例：

```yaml
actions:
  left_arm_1_joint: [-0.2, 0.0, 0.2]
  left_arm_2_joint: [-0.3, 0.0, 0.3]
  right_arm_2_joint: [-0.2, 0.0, 0.2]
```

如果测试某个关节时，需要先把同一只手臂的其他 6 个辅助关节移动到固定姿态，可以写成 `setup + positions`：

```yaml
actions:
  left_arm_2_joint:
    setup:
      left_arm_1_joint: 0.2
      left_arm_3_joint: -0.4
      left_arm_4_joint: 0.1
      left_arm_5_joint: 0.0
      left_arm_6_joint: 0.0
      left_arm_7_joint: 0.0
    positions: [-0.3, 0.0, 0.3]

  right_arm_2_joint:
    setup:
      right_arm_1_joint: 0.15
      right_arm_3_joint: -0.2
      right_arm_4_joint: 0.0
      right_arm_5_joint: 0.0
      right_arm_6_joint: 0.0
      right_arm_7_joint: 0.0
    positions: [-0.2, 0.0, 0.2]
```

`setup` 只允许写被调关节同一只手臂的辅助关节，并且必须写满除被调关节外的 6 个关节。每个 trial 前，脚本会先执行一次 setup 姿态，再只让被调关节执行 `positions` 里的测试动作并计算指标。

运行：

```bash
python3 auto_tune_arm_joints.py --action-config /path/to/kp_actions.yaml -- --kp-iterations 9
```

### 所有关节使用同一组动作

```bash
python3 auto_tune_arm_joints.py --joints all \
  --auto-tune-actions=-0.2,0.0,0.2 \
  -- --kp-iterations 9
```

### 只调指定关节

```bash
python3 auto_tune_arm_joints.py --joints left_arm_1_joint,right_arm_2_joint \
  --action-config /path/to/kp_actions.yaml \
  -- --kp-iterations 9
```

常用参数：

- `--joints others`：默认值，调除 `right_arm_1_joint` 外的 13 个关节。
- `--joints all`：调 14 个关节。
- `--continue-on-failure`：某个关节失败后继续后续关节。
- `--dry-run`：只打印将要执行的命令，不实际运动。
- `--output-dir`：结果目录，默认 `kp_auto_tune_results`。
- `--action-config`：支持旧格式 `joint: [positions]`，也支持新格式 `joint: {setup, positions}`。
- `--` 之后的参数会原样转发给 `right_1_arm.py`。

输出：

- `kp_auto_tune_results/<joint_name>/<joint_name>_kp_trials_raw.csv`
- `kp_auto_tune_results/<joint_name>/<joint_name>_kp_trials_summary.csv`
- `kp_auto_tune_results/<joint_name>/<joint_name>_auto_tune*.png`

推荐先 dry-run：

```bash
python3 auto_tune_arm_joints.py --dry-run --action-config /path/to/kp_actions.yaml -- --kp-iterations 9
```

---

## 3. `task_motion_program_kp_tune.py`
用途：按完整 motion program YAML 做任务级 `Kp` 调参。每个候选 `Kp` 都完整执行一次 YAML，并用整段任务表现打分。

适合：希望 `Kp` 优化目标和真实任务一致，而不是只看单个阶跃动作。

### 调全部 14 个关节

```bash
python3 task_motion_program_kp_tune.py /home/raybot/robot_motion_programs/your_motion_program.yaml \
  --joints all \
  --kp-iterations 9
```

也可以直接用 `/home/raybot/robot_motion_programs` 下的 YAML 名称：

```bash
python3 task_motion_program_kp_tune.py 右臂测试 --joints all --kp-iterations 9
```

### 只调指定关节

```bash
python3 task_motion_program_kp_tune.py 右臂测试 \
  --joints right_arm_1_joint,right_arm_2_joint \
  --kp-iterations 9
```

### 只监控当前被调关节

默认会监控 14 个手臂关节，并对其他关节退化加罚。如果现场只想快速验证当前关节：

```bash
python3 task_motion_program_kp_tune.py 右臂测试 \
  --joints right_arm_1_joint \
  --monitor-joints selected \
  --kp-iterations 9
```

执行流程：

- 每个 trial 先调用 `/action_recorder/edit`：`load file=<yaml>`。
- 再调用 `/action_recorder/command`：默认 `play exec=1 replan=0`。
- 执行期间采集左右臂 controller_state 的 `reference` 和 `feedback`。
- 默认每次只改当前正在调的关节 `Kp`。
- 默认调完或失败都会恢复当前关节原始 `Kp`。
- 加 `--keep-best-kp` 才会把成功搜索到的最佳 `Kp` 留在驱动里。

评分逻辑：

- 当前关节整段任务跟踪误差。
- 其他监控关节相对基线的 RMSE 退化。
- 执行失败、超时、无有效样本会加大罚分并默认停止。

常用参数：

- `--kp-iterations 9`：快速验证。
- `--task-repeats 2`：同一 `Kp` 完整任务重复两次，降低偶发噪声。
- `--play-timeout 300`：完整任务执行超时。
- `--continue-on-execution-failure`：trial 失败后继续搜索，不推荐首次使用。
- `--continue-on-joint-failure`：某个关节失败后继续调后续关节。
- `--dry-run`：只解析 YAML 和参数，不连接 ROS、不运动。
- `--play-command`：覆盖默认执行命令，例如 `"play exec=1 replan=1"`。

输出：

- `task_motion_program_kp_results/<joint_name>/<joint_name>_task_kp_trials_raw.csv`
- `task_motion_program_kp_results/<joint_name>/<joint_name>_task_kp_trials_summary.csv`
- `task_motion_program_kp_results/all_joints_task_tuning_summary.csv`

推荐先 dry-run：

```bash
python3 task_motion_program_kp_tune.py 右臂测试 --dry-run --joints right_arm_1_joint --kp-iterations 9
```

---

## 快速选择

- 只想测一个关节到某个角度：用 `right_1_arm.py`。
- 只想用 3 个明确角度调一个关节：用 `right_1_arm.py --auto-tune-kp`。
- 想批量调多个关节，但每个关节都有明确测试角度：用 `auto_tune_arm_joints.py`。
- 想用完整 YAML 任务作为评价标准：用 `task_motion_program_kp_tune.py`。

## 安全建议

- 第一次跑任何批量/任务级调参，都先加 `--dry-run`。
- 第一次真实执行建议用 `--joints <单个关节>` 和 `--kp-iterations 9`。
- 确认输出 CSV 和动作表现正常后，再扩大到多个关节。
- 不确定是否要保留新 `Kp` 时，不要加 `--keep-best-kp`。

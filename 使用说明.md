# right_1_arm.py 使用说明

## 作用

`right_1_arm.py` 用于控制右臂 `right_arm_1_joint` 运动，并基于控制器状态话题采集：

- `reference` 目标位置
- `feedback` 实际位置
- `tracking error` 跟踪误差

脚本支持两种常用模式：

- 单次运动测试
- 自动搜索位置环 `Kp`

---

## 前置条件

运行前需要满足：

- 已安装并可使用 `ROS 2 Humble`
- 已 `source` ROS 与机器人工作空间环境
- `/cli_controller/execute_command` 服务可用
- `/r_arm_controller/controller_state` 话题正常发布
- 若启用自动调参，系统中可执行 `ethercat` 命令

常用环境初始化：

```bash
source /opt/ros/humble/setup.bash
source /home/raybot/raybot_core_ws/install/setup.bash
```

进入目录：

```bash
cd /home/raybot/raybot_pid_ws
```

---

## 最常用命令

### 1. 单次运动测试

```bash
python3 right_1_arm.py 0.5
```

含义：

- 将 `right_arm_1_joint` 运动到 `0.5 rad`
- 输出 RMSE、相位滞后、幅值比、超调等指标
- 默认生成单张响应图

默认输出图：

- `right_arm_1_joint_response_0_5.png`

### 2. 自动调 Kp

```bash
python3 right_1_arm.py --auto-tune-kp
```

含义：

- 先读取当前初始 `Kp`
- 在给定区间内用一维贝叶斯优化搜索更优 `Kp`
- 运行时会依次提示输入 3 个角度，并分别执行、绘图
- 最终按这 3 个人工输入动作，各生成一张 `初始 Kp vs 最优 Kp` 对比图
- 不再在自动调参结束后额外执行命令行里的 `position`
- 所有中间候选 `Kp` 自动拆分记录到 `raw + summary` 两份 `CSV`

默认输出文件：

- `right_arm_1_joint_auto_tune_input_1_preview.png`
- `right_arm_1_joint_auto_tune_input_2_preview.png`
- `right_arm_1_joint_auto_tune_input_3_preview.png`
- `right_arm_1_joint_auto_tune_comparison_input_1.png`
- `right_arm_1_joint_auto_tune_comparison_input_2.png`
- `right_arm_1_joint_auto_tune_comparison_input_3.png`
- `kp_trials_raw.csv`
- `kp_trials_summary.csv`

---

## 输出文件说明

### 1. 单次测试模式

会生成：

- 单张响应图 `.png`

图中包含：

- Position：`reference` 与 `feedback`
- Error：`tracking error`

### 2. 自动调参模式

会生成：

- 预览图：用户每输入一个角度，就立即执行一次并生成一张单次响应图
- 对比图：按 3 个人工输入动作分别比较 `initial Kp` 和 `best Kp`
- 综合结果：脚本仅基于这 3 个输入角度的试验结果聚合选出最终最佳 `Kp`
- `kp_trials_raw.csv`：记录每个 `kp x action x repeat` 的原始 trial 明细
- `kp_trials_summary.csv`：记录每个 Kp 聚合后的摘要指标

说明：

- 自动调参模式下，不再保存中间候选的图片
- 原始试验明细与聚合摘要分开保存，便于筛选、排序和后处理
- 搜索方式为一维贝叶斯优化，不再是固定 `1/3`、`2/3` 位置的区间试探

### 3. 自动调参的搜索方式

当前 `Kp` 搜索逻辑：

1. 先读取当前驱动中的原始 `Kp`
2. 根据 `--kp-min / --kp-max` 或 `--kp-range` 确定搜索区间
3. 自动按最多 `3` 个阶段串行搜索：趋势搜索、局部精调、最终确认
4. 每个候选 `Kp` 默认重复试验 `2` 次，并用平均 `search_cost` 做比较
5. 先评估少量种子点
6. 用历史 `Kp -> search_cost` 数据拟合一维代理模型
7. 按 `Expected Improvement` 选择下一个最值得测试的 `Kp`
8. 每个阶段结束后围绕当前最优且稳定的点收窄区间，再进入下一阶段
9. 达到阶段上限或预算耗尽后输出最优结果

这样做的优点：

- 会利用历史评估结果
- 能优先探索“看起来更有希望”的区域
- 能自动从粗搜过渡到精调
- 相比固定区间搜索，对噪声更稳
- 在同样试验预算下通常更省次数

### 4. 评价体系

当前评价体系分为三层：

1. 搜索代价 `search_cost`
- 供贝叶斯优化和候选点排序使用
- 同时考虑 `rmse`、`phase lag`、`amplitude ratio` 和超调罚分

2. 阶段过渡安全 `expand_safe`
- 用于判断某个候选是否足够稳定，可作为下一阶段的收窄中心或回退候选
- 条件比最终验收更宽松

3. 严格验收 `strict_ok`
- 用于判断某个 `Kp` 是否满足最终工程标准
- 最终输出时优先选 `strict_ok=True` 的候选

说明：

- 自动调参阶段并不要求每个候选都 `strict_ok=True`
- 如果没有严格达标点，脚本会退回搜索代价最优点

---

## `kp_trials_raw.csv` 字段说明

常用字段：

- `role`
  - `baseline`：初始 Kp
  - `candidate`：中间候选 Kp
  - `best_candidate`：搜索过程中最优候选
- `kp`
- `action_label`
- `repeat_index`
- `rmse_rad`
- `phase_lag_deg`
- `amplitude_ratio`
- `overshoot_rad`
- `search_cost`
- `strict_ok`
- `expand_safe`
- `initial_position_rad`
- `target_position_rad`
- `final_position_rad`
- `sample_count`

建议重点关注：

- `search_cost`：搜索阶段总代价，越小越好
- `rmse_rad`：越小越好
- `overshoot_rad`：应尽量小
- `action_label`：区分 `input_1 / input_2 / input_3`
- `repeat_index`：定位偶发异常是否只出现在某一次重复

## `kp_trials_summary.csv` 字段说明

常用字段：

- `role`
- `kp`
- `action_label`
- `search_cost`
- `search_cost_std`
- `repeat_count`
- `strict_ok`
- `expand_safe`
- `stable_ok`
- `raw_trial_count`
- `display_action_label`

建议重点关注：

- `search_cost`：聚合后的搜索总代价，越小越好
- `search_cost_std`：同一 `Kp` 下不同动作/重复的离散程度，越小越稳定
- `repeat_count`：该 `Kp` 被实际评估的总 trial 数
- `strict_ok`：是否满足最终严格验收标准
- `expand_safe`：是否满足进入下一阶段或作为稳定回退候选的宽松安全标准

---

## 常用参数

### 运动相关

- `position`
  - 目标位置，单位 `rad`
  - 仅单次测试模式必填

- `--vel`
  - 速度缩放，默认 `0.3`

- `--acc`
  - 加速度缩放，默认 `0.3`

- `--plan-only`
  - 只规划，不执行

### 状态采样相关

- `--state-topic`
  - 控制器状态话题
  - 默认：`/r_arm_controller/controller_state`

- `--state-timeout`
  - 等待初始状态超时，默认 `2.0`

- `--observe-window`
  - 发命令后继续采样时长，默认 `3.0`

### 绘图相关

- `--plot-output`
  - 指定图片输出路径

- `--no-plot`
  - 不生成图片

### Kp 自动调参相关

- `--auto-tune-kp`
  - 启用自动调参
  - 启用后不再要求命令行提供 `position`

- `--kp-range`
  - 可选的绝对浮动量
  - 不指定时，默认以当前 `Kp` 为中心，先按 `±10%` 生成第一阶段搜索区间
  - 指定后，会覆盖默认的小区间策略

- `--kp-min`
  - 显式指定搜索下界
  - 不指定时自动由默认小区间或 `--kp-range` 推导

- `--kp-max`
  - 显式指定搜索上界
  - 不指定时自动由默认小区间或 `--kp-range` 推导

- `--kp-hard-max`
  - 自动调参允许探索的绝对硬上限
  - 默认自动推导，但会强制限制在 `1.0` 以内
  - 用于禁止无限向上外推搜索

- `--kp-iterations`
  - 三阶段自动调参流程中的总候选评估次数
  - 最小值 `9`，才能真正完成 `3` 个阶段
  - 默认 `12`
  - 预算会自动向后两个阶段倾斜，便于粗搜后继续精调

- `--kp-alias`
  - EtherCAT alias，默认 `1109`

- `--kp-index`
  - SDO index，默认 `0x2006`

- `--kp-subindex`
  - SDO subindex，默认 `0`

---

## 实用示例

### 指定更长观测时间

```bash
python3 right_1_arm.py 0.5 --observe-window 5
```

### 指定 Kp 搜索范围

默认以当前 `Kp` 为中心开始第一阶段搜索：

```bash
python3 right_1_arm.py --auto-tune-kp
```

手动指定区间：

```bash
python3 right_1_arm.py --auto-tune-kp --kp-min 0.35 --kp-max 0.48 --kp-iterations 9
```

只调整浮动量：

```bash
python3 right_1_arm.py --auto-tune-kp --kp-range 0.3
```

显式指定硬上限：

```bash
python3 right_1_arm.py --auto-tune-kp --kp-hard-max 1.0
```

增加搜索预算：

```bash
python3 right_1_arm.py --auto-tune-kp --kp-iterations 8
```

### 只测指标，不出图

```bash
python3 right_1_arm.py 0.5 --no-plot
```

### 只规划，不执行

```bash
python3 right_1_arm.py 0.5 --plan-only
```

---

## 推荐使用方式

如果目标是调 `Kp`，建议按下面顺序：

1. 先做一次单次测试，确认服务、话题、运动链路都正常
2. 再执行自动调参，先用较小预算试一次
3. 查看对比图，比较 `initial Kp` 与 `best Kp`
4. 打开 `kp_trials_summary.csv`，按 `search_cost`、`rmse_rad`、`overshoot_rad` 筛选中间候选
5. 若要复盘噪声、方向差异或偶发超调，再查看 `kp_trials_raw.csv`
6. 若结果仍不稳定，再适当增大 `--kp-iterations`

建议：

- 初次调参可先用默认 `--kp-iterations 12`
- 如果只想快速验证，可用 `9`
- 如果搜索区间较宽，可提高到 `12 ~ 15`
- 如果机械臂现场波动较大，优先缩小合理搜索区间，再增加迭代次数
- 当前默认策略会先在当前区间内搜索，再围绕稳定候选逐阶段收窄区间
- 默认最多执行 `3` 个阶段，并始终受当前推导出的硬上限约束

---

## 常见问题

### 1. 提示无法导入 ROS 依赖

通常是没有 `source` 环境：

```bash
source /opt/ros/humble/setup.bash
source /home/raybot/raybot_core_ws/install/setup.bash
```

### 2. 等待 controller_state 超时

重点检查：

- `/r_arm_controller/controller_state` 是否存在
- 控制器是否已启动
- `joint_names` 中是否包含 `right_arm_1_joint`

### 3. 自动调参时报 ethercat 错误

重点检查：

- `ethercat` 命令是否可执行
- alias / index / subindex 是否正确
- 当前驱动是否允许读写对应参数

### 4. 为什么自动调参没有保存很多图片

这是当前设计行为：

- 先生成 3 张人工输入动作的预览图，再生成 3 张对应动作的对比图
- 其他候选统一记录到 `kp_trials_raw.csv` 和 `kp_trials_summary.csv`

这样更便于比较和归档，不会产生大量低价值图片。

### 5. `--kp-iterations` 调大后为什么耗时更长

这是正常现象。

因为自动调参每多评估一个候选 `Kp`，都需要：

- 回到统一起点
- 写入一次驱动参数
- 再执行一次完整运动和观测

所以：

- `--kp-iterations` 越大，搜索通常越充分
- 但总试验耗时也会线性增加

### 6. 为什么日志里会看到阶段收窄

这是当前自动调参的设计行为。

脚本会先在当前阶段区间内搜索；当阶段结束后，会围绕当前最优且稳定的候选自动收窄区间，再进入下一阶段。

这样可以避免一开始就在过大区间内盲目试探，同时保留从粗搜逐步过渡到精调的能力。

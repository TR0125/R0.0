# right_1_arm.py 使用说明

## 作用

`right_1_arm.py` 用于控制右臂 `right_arm_1_joint` 运动，并基于控制器状态话题采集：

- `reference` 目标位置
- `feedback` 实际位置
- `tracking error` 跟踪误差

脚本支持两种常用模式：

- 单次运动测试
- 自动搜索位置环 `Kp`

---

## 前置条件

运行前需要满足：

- 已安装并可使用 `ROS 2 Humble`
- 已 `source` ROS 与机器人工作空间环境
- `/cli_controller/execute_command` 服务可用
- `/r_arm_controller/controller_state` 话题正常发布
- 若启用自动调参，系统中可执行 `ethercat` 命令

常用环境初始化：

```bash
source /opt/ros/humble/setup.bash
source /home/raybot/raybot_core_ws/install/setup.bash
```

进入目录：

```bash
cd /home/raybot/raybot_pid_ws
```

---

## 最常用命令

### 1. 单次运动测试

```bash
python3 right_1_arm.py 0.5
```

含义：

- 将 `right_arm_1_joint` 运动到 `0.5 rad`
- 输出 RMSE、相位滞后、幅值比、超调等指标
- 默认生成单张响应图

默认输出图：

- `right_arm_1_joint_response_0_5.png`

### 2. 自动调 Kp

```bash
python3 right_1_arm.py 0.5 --auto-tune-kp
```

含义：

- 先读取当前初始 `Kp`
- 在给定区间内搜索更优 `Kp`
- 最终只生成 `初始 Kp vs 最优 Kp` 对比图
- 所有中间候选 `Kp` 自动记录到 `CSV`

默认输出文件：

- `right_arm_1_joint_response_0_5_comparison.png`
- `kp_trials.csv`

---

## 输出文件说明

### 1. 单次测试模式

会生成：

- 单张响应图 `.png`

图中包含：

- Position：`reference` 与 `feedback`
- Error：`tracking error`

### 2. 自动调参模式

会生成：

- 对比图：只比较 `initial Kp` 和 `best Kp`
- `kp_trials.csv`：记录中间候选 `Kp` 的运行效果

说明：

- 自动调参模式下，不再保存中间候选的图片
- 中间候选通过 `CSV` 保存，便于筛选、排序和后处理

---

## kp_trials.csv 字段说明

常用字段：

- `role`
  - `baseline`：初始 Kp
  - `candidate`：中间候选 Kp
  - `best_candidate`：搜索过程中最优候选
- `kp`
- `rmse_rad`
- `phase_lag_deg`
- `amplitude_ratio`
- `overshoot_rad`
- `cost`
- `meets_target`
- `initial_position_rad`
- `target_position_rad`
- `final_position_rad`
- `sample_count`

建议重点关注：

- `cost`：越小越好
- `rmse_rad`：越小越好
- `overshoot_rad`：应尽量为 `0`
- `meets_target`：是否满足目标要求

---

## 常用参数

### 运动相关

- `position`
  - 目标位置，单位 `rad`

- `--vel`
  - 速度缩放，默认 `0.3`

- `--acc`
  - 加速度缩放，默认 `0.3`

- `--plan-only`
  - 只规划，不执行

### 状态采样相关

- `--state-topic`
  - 控制器状态话题
  - 默认：`/r_arm_controller/controller_state`

- `--state-timeout`
  - 等待初始状态超时，默认 `2.0`

- `--observe-window`
  - 发命令后继续采样时长，默认 `3.0`

### 绘图相关

- `--plot-output`
  - 指定图片输出路径

- `--no-plot`
  - 不生成图片

### Kp 自动调参相关

- `--auto-tune-kp`
  - 启用自动调参

- `--kp-min`
  - 搜索下界，默认 `0.2`

- `--kp-max`
  - 搜索上界，默认 `0.5`

- `--kp-iterations`
  - 搜索轮数，默认 `4`

- `--kp-alias`
  - EtherCAT alias，默认 `1109`

- `--kp-index`
  - SDO index，默认 `0x2006`

- `--kp-subindex`
  - SDO subindex，默认 `0`

---

## 实用示例

### 指定更长观测时间

```bash
python3 right_1_arm.py 0.5 --observe-window 5
```

### 指定 Kp 搜索范围

```bash
python3 right_1_arm.py 0.5 --auto-tune-kp --kp-min 0.35 --kp-max 0.48 --kp-iterations 5
```

### 只测指标，不出图

```bash
python3 right_1_arm.py 0.5 --no-plot
```

### 只规划，不执行

```bash
python3 right_1_arm.py 0.5 --plan-only
```

---

## 推荐使用方式

如果目标是调 `Kp`，建议按下面顺序：

1. 先做一次单次测试，确认服务、话题、运动链路都正常
2. 再执行自动调参
3. 查看对比图，比较 `initial Kp` 与 `best Kp`
4. 打开 `kp_trials.csv`，按 `cost`、`rmse_rad`、`overshoot_rad` 筛选中间候选

---

## 常见问题

### 1. 提示无法导入 ROS 依赖

通常是没有 `source` 环境：

```bash
source /opt/ros/humble/setup.bash
source /home/raybot/raybot_core_ws/install/setup.bash
```

### 2. 等待 controller_state 超时

重点检查：

- `/r_arm_controller/controller_state` 是否存在
- 控制器是否已启动
- `joint_names` 中是否包含 `right_arm_1_joint`

### 3. 自动调参时报 ethercat 错误

重点检查：

- `ethercat` 命令是否可执行
- alias / index / subindex 是否正确
- 当前驱动是否允许读写对应参数

### 4. 为什么自动调参没有保存很多图片

这是当前设计行为：

- 只保留 `初始 Kp` 和 `最优 Kp` 的对比图
- 其他候选统一记录到 `kp_trials.csv`

这样更便于比较和归档，不会产生大量低价值图片。

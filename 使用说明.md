# PID 调参脚本使用说明

本文档适用于 `/home/raybot/raybot_pid_ws` 下的 3 个脚本：

- `right_1_arm.py`：单关节运动测试，或单关节基于指定目标角度自动搜索 `Kp`。
- `auto_tune_arm_joints.py`：批量顺序调用 `right_1_arm.py`，给多个关节做单关节 `Kp` 调参。
- `task_motion_program_kp_tune.py`：执行完整 motion program YAML，用整段任务表现评价并搜索 `Kp`。

## 1. 运行前检查

先进入脚本目录并加载 ROS 环境：

```bash
cd /home/raybot/raybot_pid_ws
source /opt/ros/humble/setup.bash
source /home/raybot/raybot_core_ws/install/setup.bash
```

确认基础服务和话题：

```bash
ros2 service list | grep /cli_controller/execute_command
ros2 topic list | grep -E '/r_arm_controller/controller_state|/l_arm_controller/controller_state'
```

如果要自动调 `Kp`，还需要确认 `ethercat` 命令可用，并且当前用户有权限读写驱动 SDO：

```bash
which ethercat
```

如果要按完整 motion program YAML 调参，还需要确认：

```bash
ros2 service list | grep /action_recorder/edit
ros2 action list | grep /action_recorder/command
```

关节名和 EtherCAT alias 自动映射：

- 左臂：`left_arm_1_joint` 到 `left_arm_7_joint`，alias 为 `1102..1108`。
- 右臂：`right_arm_1_joint` 到 `right_arm_7_joint`，alias 为 `1109..1115`。

## 2. 如何选择脚本

- 只测一个关节到某个角度：用 `right_1_arm.py`。
- 只调一个关节，测试动作是几个明确目标角度：用 `right_1_arm.py --auto-tune-kp`。
- 批量调多个关节，每个关节都有明确测试角度：用 `auto_tune_arm_joints.py`。
- 希望优化目标贴近真实任务表现：用 `task_motion_program_kp_tune.py`。

`right_1_arm.py` 和 `auto_tune_arm_joints.py` 评价的是单关节动作响应；`task_motion_program_kp_tune.py` 评价的是完整 YAML 任务执行期间的跟踪误差和其他关节退化。

## 3. 单关节测试：`right_1_arm.py`

### 单次运动

默认控制 `right_arm_1_joint`：

```bash
python3 right_1_arm.py 0.5
```

指定其他关节：

```bash
python3 right_1_arm.py --joint-name left_arm_3_joint 0.3
```

常用参数：

- `--joint-name`：目标关节，默认 `right_arm_1_joint`。
- `--vel`：速度缩放，默认 `0.3`。
- `--acc`：加速度缩放，默认 `0.3`。
- `--state-topic`：覆盖 controller_state 话题，通常不用手动填。
- `--observe-window`：命令完成后继续采样的时间，默认 `3.0` 秒。
- `--no-plot`：只打印指标，不保存响应图。
- `--plot-output`：指定响应图路径。

输出包含：

- 实际下发给 `/cli_controller/execute_command` 的命令。
- `rmse`、`phase_lag`、`amplitude_ratio`、`overshoot`、`tail_std`、`search_cost` 等指标。
- 默认响应图：`<joint_name>_response_<target>.png`，例如 `left_arm_3_joint_response_0_3.png`。

### 单关节自动调 Kp

交互输入目标角度。交互模式固定提示 3 个动作：

```bash
python3 right_1_arm.py --joint-name right_arm_2_joint --auto-tune-kp
```

非交互指定目标角度。这里可以给任意非空数量，现场建议先用 3 个覆盖低、中、高位置的目标：

```bash
python3 right_1_arm.py --joint-name right_arm_2_joint --auto-tune-kp \
  --auto-tune-actions=-0.2,0.0,0.2 \
  --kp-iterations 9
```

如果测试前要把同臂其他 6 个辅助关节放到固定姿态，使用 `--setup-joints`。必须写满除被调关节外的 6 个同臂关节：

```bash
python3 right_1_arm.py --joint-name left_arm_2_joint --auto-tune-kp \
  --setup-joints=left_arm_1_joint:0,left_arm_3_joint:-0.4,left_arm_4_joint:0,left_arm_5_joint:0,left_arm_6_joint:0,left_arm_7_joint:0 \
  --auto-tune-actions=-0.3,0.0,0.3 \
  --kp-iterations 9
```

常用搜索参数：

- `--kp-iterations 9`：最低预算，适合第一次快速验证。
- `--kp-iterations 12`：默认预算。
- `--kp-min / --kp-max`：手动限定搜索区间。
- `--kp-range`：围绕当前 `Kp` 给定绝对搜索半径。
- `--kp-hard-max`：限制最高可试 `Kp`；不指定时自动从当前 `Kp` 推导，并强制不超过 `1.0`。
- `--kp-raw-csv-output`：指定原始 trial CSV。
- `--kp-summary-csv-output`：指定聚合摘要 CSV。

输出文件：

- `kp_trials_raw.csv` 或 `<joint_name>_kp_trials_raw.csv`：每个 `Kp x action x repeat` 的原始 trial。
- `kp_trials_summary.csv` 或 `<joint_name>_kp_trials_summary.csv`：每个候选 `Kp` 的聚合结果。
- `*_preview.png`：原始 `Kp` 的动作预览图。
- `*_comparison_*.png`：原始 `Kp` 与最佳 `Kp` 的对比图。

注意：`right_1_arm.py --auto-tune-kp` 在结束时会恢复原始 `Kp`，脚本没有提供保留最佳 `Kp` 的参数。需要保留最佳值时，先查看终端输出的 `kp_selected`，再用现场允许的 EtherCAT 写入流程手动确认。

## 4. 批量单关节调参：`auto_tune_arm_joints.py`

这个脚本本身不直接控制机器人，而是按关节顺序生成并执行多条 `right_1_arm.py --auto-tune-kp` 命令。

### 推荐配置文件

仓库里已有示例：

```bash
raybot_pid_ws/kp_actions_with_setup.yaml
```

配置格式：

```yaml
actions:
  left_arm_1_joint:
    setup:
      left_arm_2_joint: 0.0
      left_arm_3_joint: 0.0
      left_arm_4_joint: 0.0
      left_arm_5_joint: 0.0
      left_arm_6_joint: 0.0
      left_arm_7_joint: 0.0
    positions: [-0.5, 0.5, 1.0]

  right_arm_2_joint:
    setup:
      right_arm_1_joint: 0.0
      right_arm_3_joint: 0.0
      right_arm_4_joint: 0.0
      right_arm_5_joint: 0.0
      right_arm_6_joint: 0.0
      right_arm_7_joint: 0.0
    positions: [0.5, 1.57, 0.2]
```

也支持旧格式：

```yaml
actions:
  left_arm_1_joint: [-0.2, 0.0, 0.2]
```

`setup` 规则：

- 只允许写目标关节同一只手臂的辅助关节。
- 必须写满除目标关节外的 6 个关节。
- 每个 trial 前先执行 setup 姿态，再只让目标关节执行 `positions` 中的动作。

### 先 dry-run

```bash
python3 auto_tune_arm_joints.py --dry-run \
  --action-config kp_actions_with_setup.yaml \
  -- --kp-iterations 9 --no-plot
```

`--` 后面的参数会原样转发给 `right_1_arm.py`。

### 真实执行

按配置文件调参，默认 `--joints others`，即除 `right_arm_1_joint` 外的 13 个关节：

```bash
python3 auto_tune_arm_joints.py \
  --action-config kp_actions_with_setup.yaml \
  -- --kp-iterations 9
```

调全部 14 个关节：

```bash
python3 auto_tune_arm_joints.py --joints all \
  --action-config kp_actions_with_setup.yaml \
  -- --kp-iterations 9
```

只调指定关节：

```bash
python3 auto_tune_arm_joints.py \
  --joints left_arm_1_joint,right_arm_2_joint \
  --action-config kp_actions_with_setup.yaml \
  -- --kp-iterations 9
```

不给配置文件时，所有选中关节使用同一组动作：

```bash
python3 auto_tune_arm_joints.py --joints all \
  --auto-tune-actions=-0.2,0.0,0.2 \
  -- --kp-iterations 9
```

常用参数：

- `--joints others`：默认值，调除 `right_arm_1_joint` 外的 13 个关节。
- `--joints all`：调全部 14 个关节。
- `--joints left_arm_1_joint,right_arm_2_joint`：只调指定关节。
- `--continue-on-failure`：某个关节失败后继续后续关节。
- `--dry-run`：只打印将要执行的命令，不实际运动。
- `--output-dir`：结果目录，默认 `kp_auto_tune_results`。
- `--python`：指定启动子脚本的 Python 解释器。

输出目录：

- `kp_auto_tune_results/<joint_name>/<joint_name>_kp_trials_raw.csv`
- `kp_auto_tune_results/<joint_name>/<joint_name>_kp_trials_summary.csv`
- `kp_auto_tune_results/<joint_name>/<joint_name>_auto_tune*.png`

注意：由于子脚本会恢复原始 `Kp`，批量脚本默认也不会把最佳 `Kp` 留在驱动里。

## 5. 完整任务调参：`task_motion_program_kp_tune.py`

这个脚本每个候选 `Kp` 都会完整执行一次 motion program YAML，并用整段任务表现打分。适合最终确认真实任务表现。

### dry-run

先确认 YAML 路径、关节选择、监控关节和输出目录：

```bash
python3 task_motion_program_kp_tune.py 右臂测试 \
  --dry-run \
  --joints right_arm_1_joint \
  --kp-iterations 9
```

可以直接给绝对路径，也可以给 `/home/raybot/robot_motion_programs` 下的 YAML 名称。不带 `.yaml` 后缀时，脚本会自动补齐查找。

注意：motion program 路径不能包含空格，因为 `/action_recorder/edit` 使用 `load file=<path>`，路径里有空格会解析失败。

### 调全部关节

```bash
python3 task_motion_program_kp_tune.py /home/raybot/robot_motion_programs/your_motion_program.yaml \
  --joints all \
  --kp-iterations 9
```

### 只调指定关节

```bash
python3 task_motion_program_kp_tune.py 右臂测试 \
  --joints right_arm_1_joint,right_arm_2_joint \
  --kp-iterations 9
```

### 只监控当前被调关节

默认 `--monitor-joints all`，会监控 14 个手臂关节，并对其他关节相对基线的 RMSE 退化加罚。快速验证当前关节时可以改为：

```bash
python3 task_motion_program_kp_tune.py 右臂测试 \
  --joints right_arm_1_joint \
  --monitor-joints selected \
  --kp-iterations 9
```

执行流程：

- 每个 trial 先调用 `/action_recorder/edit`：`load file=<yaml>`。
- 再调用 `/action_recorder/command`：默认命令为 `play exec=1 replan=0`。
- 执行期间采集 `/l_arm_controller/controller_state` 和 `/r_arm_controller/controller_state` 的 `reference` 与 `feedback`。
- 每次只改当前正在调的关节 `Kp`。
- 默认调完或失败都会恢复当前关节原始 `Kp`。
- 只有加 `--keep-best-kp`，且该关节调参成功，才会把最佳 `Kp` 留在驱动里。

评分逻辑：

- 当前被调关节的 `rmse`、`mae`、最大绝对误差、尾段误差标准差。
- 其他监控关节相对基线的 RMSE 退化。
- 执行失败、超时、无有效样本会加 `--failure-penalty`，默认失败后停止当前搜索。

常用参数：

- `--kp-iterations 9`：最低预算，适合第一次快速验证。
- `--kp-iterations 12`：默认预算。
- `--task-repeats 2`：同一 `Kp` 完整任务重复两次，降低偶发噪声。
- `--play-timeout 300`：完整任务执行超时。
- `--post-observe-window 1.0`：任务完成后继续采样时间。
- `--continue-on-execution-failure`：trial 失败后继续搜索，首次使用不建议加。
- `--continue-on-joint-failure`：某个关节失败后继续调后续关节。
- `--keep-best-kp`：成功时保留最佳 `Kp`。
- `--play-command "play exec=1 replan=1"`：覆盖默认执行命令。

输出文件：

- `task_motion_program_kp_results/<joint_name>/<joint_name>_task_kp_trials_raw.csv`
- `task_motion_program_kp_results/<joint_name>/<joint_name>_task_kp_trials_summary.csv`
- `task_motion_program_kp_results/all_joints_task_tuning_summary.csv`

## 6. 安全建议

- 第一次跑批量或任务级调参，必须先 `--dry-run` 看清楚将要调哪些关节。
- 第一次真实执行建议只选 1 个关节，并使用 `--kp-iterations 9`。
- 批量调参建议先加 `--continue-on-failure` 做摸底，再根据失败原因单独处理。
- 不确定是否保留新 `Kp` 时，不要给 `task_motion_program_kp_tune.py` 加 `--keep-best-kp`。
- 输出 CSV 正常、动作表现正常、无异常抖动后，再扩大到多个关节或提高 `--kp-iterations`。
- `--kp-hard-max` 不要随意放大；代码默认把自动推导的硬上限夹到 `<= 1.0`。

## 7. 常见问题

### 提示 ROS 依赖导入失败

重新加载环境：

```bash
source /opt/ros/humble/setup.bash
source /home/raybot/raybot_core_ws/install/setup.bash
```

### 提示 `ethercat command not found`

确认 EtherCAT 工具安装和 PATH：

```bash
which ethercat
```

### 提示 controller_state 超时

检查对应手臂控制器是否在发布状态：

```bash
ros2 topic echo --once /r_arm_controller/controller_state
ros2 topic echo --once /l_arm_controller/controller_state
```

### motion program YAML 找不到

使用绝对路径，或确认文件位于 `/home/raybot/robot_motion_programs`：

```bash
ls /home/raybot/robot_motion_programs
```
